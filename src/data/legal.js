/**
 * Who owns this, in one place.
 *
 * The start screen shows it and the LICENSE file at the root of the repository
 * says it in full. Both should say the same thing, so the year and the holder
 * are written here once — change them here and the game follows.
 */

/** The year the work was first published. */
export const COPYRIGHT_YEAR = 2026;

/** The rights holder. Replace with a legal name if one should appear instead. */
export const COPYRIGHT_HOLDER = 'zaniixx';

/** The line shown at the foot of the start screen. */
export const COPYRIGHT =
  `© ${COPYRIGHT_YEAR} ${COPYRIGHT_HOLDER}. All rights reserved.`;
